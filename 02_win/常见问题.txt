1.windows某个文件夹被程序占用，怎么找到并删除：
按 Win+R → 输入 resmon → 回车（或任务管理器→性能→打开资源监视器）
切换到 CPU 选项卡 → 找到 关联的句柄 搜索框
粘贴 / 输入文件夹路径（如 D:\Temp\MyFolder）或文件夹名
结果里看 Image（进程名）、PID（进程 ID），就是占用程序
右键进程 → 结束进程（谨慎结束系统进程如 explorer.exe、svchost.exe）

